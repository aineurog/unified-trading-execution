"""Example: custom risk validators (v2 — extensibility demonstration).

v1 ships with a fixed chain of validators. v2 will allow integrators to
plug in custom validators via the same chain interface.
"""

from __future__ import annotations
