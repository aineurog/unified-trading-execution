[project]
name = "unified-trading-execution-metatrader"
version = "0.1.0"
description = "Unified Trading Execution — MetaTrader 5 adapter"
requires-python = ">=3.11"
license = {text = "Apache-2.0"}
authors = [
    {name = "Syed Qaisar Jalil"},
]
dependencies = [
    "unified-trading-execution>=0.1.0",
]

[project.optional-dependencies]
mt5 = [
    "MetaTrader5>=5.0.0",
]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
]

[tool.setuptools.packages.find]
where = ["src"]
